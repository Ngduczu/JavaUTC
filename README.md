# Công nghệ Java

### **|** Tác giả : *Nguyễn Đức Vũ - Công nghệ thông tin 4 - Khóa 65 - Trường Đại học Giao Thông Vận Tải* 

*Đây là repository được sinh ra nhằm mục đích lưu giữ kiến thức và note Java kì học số 4 của DucZu tại UTC*

<br>

- `Source Code` sẽ được lưu trữ **[ở đây](./src/)**
- `Ghi chép kiến thức` sẽ được lưu giữ **[ở đây](./note/)**

*Mỗi một `package` bên trong folder `src` sẽ là một phần nội dung kiến thức được note do DucZu trong quá trình tự học và tên của `package` đó sẽ trùng với nội dung kiến thức luôn - phần note kiến thức trong folder `note` cũng sẽ trùng với nội dung mà note đó nhắc đến, nếu 1 có folder trong folder `note` thì tức là nội dung phần kiến thức đó đã được chia nhỏ để dễ tìm hiểu.*

<br>

*Quy ước đặt tên trong repository mà có thể tôi sẽ tuân theo hoặc là đéo 🤡🤡🤡*
<br>
- *Tên `package` - tất cả in thường, không dấu, không gạch ngang, gạch dưới*
- *Tên `Class, Inteface, Enum` - Dùng PascalCase, danh từ, riêng `Interface` thì dùng tính chất, hành vi, rõ nghĩa* 
- *Tên `Hàm` thường sẽ là camelCase - bắt đầu bằng động từ - tên hàm thể hiện mục đích tạo ra hàm*
- *Tên `Biến` thì sẽ là camelCase - dùng danh từ*
- *Tên `Hằng số` thì sẽ viết in hoa toàn bộ, dùng UPPER_SNAKE_CASE, dùng `static final`*
- *Tên `Biến boolean` - bắt đầu bằng `is`, `has`, `can`, `should`*
<br><br>

**|** *Mọi thắc mắc liên hệ:*
- [`Đường link FaceBook`](https://facebook.com/ndv128)
- [`Email liên hệ`](mailto:ndv120806@gmail.com)
